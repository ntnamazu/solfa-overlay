# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール

- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### 実装可能なタスクのみを計画

- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### タスクスキップが許可される唯一のケース

以下の技術的理由に該当する場合のみスキップ可能:

- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:

```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

### タスクが大きすぎる場合

- タスクを小さなサブタスクに分割
- 分割したサブタスクをこのファイルに追加
- サブタスクを1つずつ完了させる

---

## 前提: 作業ブランチ

- [x] `chore/release-version-guard` ブランチを `develop` から作成する
  - `docs/development-guidelines.md`「ブランチ戦略」に従う（CI 設定の変更のためコミット type は `chore`、既存の `docs/ui-wording-improvement` と同様に type をブランチ接頭辞に用いる）

## フェーズ1: 検証ステップの追加

- [x] `.github/workflows/release.yml` に検証ステップを追加する
  - [x] `actions/setup-node@v4` の直後・`npm ci` の前に挿入する
  - [x] `shell: bash` を明示する（`windows-latest` の既定は PowerShell のため）
  - [x] `node -p "require('./package.json').version"` で version を取得する
  - [x] `"${GITHUB_REF_NAME#v}"` でタグ名から先頭の `v` を除去する
  - [x] タグ名は環境変数 `GITHUB_REF_NAME` 経由で参照する（`github.ref_name` のテンプレート補間を `run:` に直接埋め込まない＝スクリプトインジェクション対策）
  - [x] 不一致時に `::error::` でタグ側と `package.json` 側の**両方の値**を出力し `exit 1` する
  - [x] 一致時は確認用のログを出力する
  - [x] ステップの意図（バージョンの唯一の情報源が `package.json` であること・重いステップの前に落とす理由）をコメントで残す

## フェーズ2: 検証ステップの動作確認

- [x] `release.yml` の YAML 構文が妥当であることを確認する
  - [x] YAML パーサ（`js-yaml` または Python の `yaml`）でパースが成功する
  - [x] パース結果上、検証ステップが `npm ci` より前に位置することを確認する（guard=2 / npm ci=3 / fetch-resources=4 / dist:win=5）
  - [x] パース結果から `shell: bash` が設定されていることを確認する
- [x] 検証ロジックをローカルで模擬実行する
  - [x] **yaml から `run:` の本文を抽出して実行する**（手でコピーした別物を検証しないため）
  - [x] ケース1: `GITHUB_REF_NAME=v0.1.0`（一致）→ 終了コード 0・一致ログ
  - [x] ケース2: `GITHUB_REF_NAME=v0.2.0`（不一致）→ 終了コード 1・`::error::` に両方の値
  - [x] ケース3: `GITHUB_REF_NAME=0.1.0`（`v` なし）→ 終了コード 0
  - [x] ケース4（追加）: `GITHUB_REF_NAME=v0.1.0-beta.1`（プレリリース・package.json は `0.1.0`）→ 終了コード 1。プレリリースタグでも `package.json` 側を同じ文字列にする必要があることを確認

## フェーズ3: README のリリース手順更新

- [x] README「リリース手順」節を更新する
  - [x] `package.json` の version を先に上げてからタグを打つ順序を明記する
  - [x] `npm version <patch|minor|major>` でコミットと `v` 付きタグが生成されることを記載する
  - [x] `git push --follow-tags` でコミットとタグをまとめて push する手順にする
  - [x] タグと `package.json` が不一致の場合、ワークフローが**ビルド前に失敗する**旨を記載する
  - [x] 画面表示・インストーラ名・exe プロパティが `package.json` の version に由来することに触れる

## フェーズ4: 品質チェックと修正

- [x] リントエラーがないことを確認
  - [x] `npm run lint`
- [x] 型エラーがないことを確認
  - [x] `npm run typecheck`
- [x] すべてのテストが通ることを確認
  - [x] `npm test` → 46 ファイル / 770 テスト すべてパス
- [x] 受け入れ条件（`requirements.md`）を1件ずつ照合する
  - [x] `release.yml` 側の 7 条件を `js-yaml` でパースして機械照合（すべて充足）
  - [x] README 側の 3 条件を `grep` で照合（すべて充足）
  - [x] 変更ファイルの `npx prettier --check` を実施。README の警告は**変更前から存在する既存の未整形**（「動作要件」表の桁揃え）であり、今回の追加分に起因しないことを `git show HEAD:README.md` との比較で確認。今回の diff を汚さないため修正しない（`npm run lint` は eslint のみで prettier は CI のゲートに入っていない）

## フェーズ5: ドキュメント更新

- [ ] 変更内容をコミットする（`chore(release): タグと package.json の version 一致を CI で検証する`）
- [ ] 実装後の振り返り（このファイルの下部に記録）

---

## 補足: 本タスクで `npm run build` を実行しない理由

`docs/` 配下のテンプレートでは品質チェックに `npm run build` が含まれるが、本タスクの変更対象は `.github/workflows/release.yml` と `README.md` のみで、`src/` 配下のコードを一切変更しない。ビルド成果物に影響しないため実行対象から外す（lint / typecheck / test は既存の壊れがないことの確認として実施する）。

---

## 実装後の振り返り

### 実装完了日

{YYYY-MM-DD}

### 計画と実績の差分

**計画と異なった点**:

- {計画時には想定していなかった技術的な変更点}

**新たに必要になったタスク**:

- {実装中に追加したタスク}

### 学んだこと

**技術的な学び**:

- {実装を通じて学んだ技術的な知見}

**プロセス上の改善点**:

- {タスク管理で良かった点}

### 次回への改善提案

- {次回の作業で気をつけること}
